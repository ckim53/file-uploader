<div class="flex h-screen">
	<!-- sidebar -->
	<aside class="w-56 bg-gray-900 text-white p-5">
		<h2 class="text-xl font-bold mb-6">CloudVault</h2>
		<a href="/dashboard" class="block mb-2 hover:text-gray-300">All Files</a>
	</aside>

	<!-- main -->
	<div class="flex-1 flex flex-col">
		<!-- topbar -->
		<header class="flex justify-between items-center p-4 border-b">
			<div>
				<h2 class="text-xl font-semibold"><%= folder.name %></h2>
				<p class="text-sm text-gray-500">Folder</p>
			</div>

			<div class="flex gap-2">
				<a
					href="/dashboard/folders/<%=folder.id%>/upload"
					class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600"
				>
					Upload
				</a>
				<a
					href="/dashboard/folders/<%=folder.id%>/new-folder"
					class="bg-gray-200 px-4 py-2 rounded hover:bg-gray-300"
				>
					New Folder
				</a>
			</div>
		</header>

		<!-- content -->
		<div class="p-6">
			<!-- edit name -->
			<div class="mb-6">
				<button id="edit-btn" class="text-sm text-blue-600 hover:underline">
					Rename folder
				</button>

				<form
					id="edit-form"
					action="/dashboard/folders/<%= folder.id %>/edit?_method=PUT"
					method="POST"
					class="mt-3 hidden flex gap-2"
				>
					<input
						type="text"
						name="name"
						value="<%= folder.name %>"
						class="border p-2 rounded w-64"
						required
					/>
					<button class="bg-blue-500 text-white px-3 py-1 rounded">Save</button>
					<button type="button" id="cancel-btn">Cancel</button>
				</form>
			</div>

			<!-- folders -->
			<h3 class="text-sm font-semibold text-gray-500 uppercase mb-2">
				Folders
			</h3>
			<div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4 mb-8">
				<% folderChildren.forEach(folderChild => { %>
				<a
					href="/dashboard/folders/<%= folderChild.id %>"
					class="bg-cyan-100 p-4 rounded-lg hover:bg-cyan-200 text-center"
				>
					<div class="mt-2 text-sm truncate"><%= folderChild.name %></div>
				</a>
				<% }) %>
			</div>

			<!-- files -->
			<h3 class="text-sm font-semibold text-gray-500 uppercase mb-2">Files</h3>
			<div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
				<% fileChildren.forEach(fileChild => { %>
				<a
					href="/dashboard/folders/<%= fileChild.id %>/details"
					class="bg-blue-100 p-4 rounded-lg hover:bg-blue-200 text-center"
				>
					<div class="mt-2 text-sm truncate"><%= fileChild.name %></div>
				</a>
				<% }) %>
			</div>

			<!-- delete -->
			<div class="mt-10">
				<form
					action="/dashboard/folders/<%= folder.id %>?_method=DELETE"
					method="POST"
					onsubmit="return confirm('Delete this folder and all its contents?');"
				>
					<button class="text-red-500 text-sm hover:underline">
						Delete folder
					</button>
				</form>
			</div>
		</div>
	</div>
</div>

<script>
	const editBtn = document.getElementById('edit-btn');
	const editForm = document.getElementById('edit-form');
	const cancelBtn = document.getElementById('cancel-btn');

	editBtn.addEventListener('click', () => {
		editForm.classList.remove('hidden');
		editBtn.classList.add('hidden');
	});

	cancelBtn.addEventListener('click', () => {
		editForm.classList.add('hidden');
		editBtn.classList.remove('hidden');
	});
</script>
